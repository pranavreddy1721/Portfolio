Drop your project screenshots right here, with these exact filenames
(matching what's already set in ../projects/projects.json):

  alumni-portal.png
  dental-clinic.png
  task-manager.png
  resume-pro.png

Tips for best results:
- Take the screenshot of each site's homepage/dashboard
- Landscape orientation works best (roughly 16:10, e.g. 1280x800px)
- The image will auto-crop to fill the laptop screen frame, anchored to the
  top of the image (so keep the top of your screenshot as the important part)

Until a file is added, that project card will show a placeholder code icon
inside the laptop screen instead of a missing image.
